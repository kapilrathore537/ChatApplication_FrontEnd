import { SocketMessageResponse } from './socket-message-response';

describe('SocketMessageResponse', () => {
  it('should create an instance', () => {
    expect(new SocketMessageResponse()).toBeTruthy();
  });
});
